# 项目开发守则

本文件适用于仓库内所有修改。完整说明见 `docs/appendix/development-rules.md`。

## 必须遵守

- 所有 Markdown 文档使用中文，正式文档放在 `docs/`。
- 运行、构建、检查、预览和发布只能使用 `scripts/*.sh`。
- 修改前先写清目标、修改范围和可验证的完成标准。
- 只改完成任务所必需的文件，不顺手重构无关代码。
- 优先复用现有 VitePress 能力；重复结构出现两次以上再抽成 Vue 组件。
- 页面专属样式必须通过 `pageClass` 或唯一组件类隔离，禁止污染全局主题。
- 外部文字、图片和代码必须先确认许可证；无明确授权时只做独立总结并标注来源。
- 不得把密钥、令牌、个人信息或生产数据写入代码、文档和提交记录。
- 合入前至少运行 `./scripts/check.sh` 与 `./scripts/audit.sh`。
- 发布后必须检查线上页面、CSS/JS 资源和深层路由，不能只依据命令成功输出。

## 视觉基线

- 保持参考站的 64px 顶栏、三栏文档布局、紧凑侧栏和蓝色品牌色。
- 新页面必须验证桌面与移动端、浅色与深色模式。
- 交互控件需要语义标签、键盘可操作状态和清晰的焦点反馈。
